# The token to your Telegram Bot. Can be obtained from @BotFather on Telegram

BOT_TOKEN=YourBotTokenHere

# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

DATABASE_URL=file:./dev.db

# Rate limiter settings
# Telegram has a limit of 30 messages per second so these default values should work fine.
# Adjust in case you have a premium bot which allows for higher limits of up to 1000 messages per second.

LIMITER_INTERVAL=1000

LIMITER_INTERVAL_CAP=30

# Cron schedule for dispatching queued messages
# Defaults to every 30 seconds. Adjust as needed.

DISPATCH_CRON=*/30 * * * * *

# Cron schedule for queuing out of range alerts
# Defaults to every hour. Adjust as needed.

OUT_OF_RANGE_CRON=0 * * * *
